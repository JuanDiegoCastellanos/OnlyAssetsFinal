@model Role

@{
    ViewData["Title"] = Model.RoleType;
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p><h1>Details for @Model.RoleType</h1></p>
        <div class="row">
            <div class="col-md-8 offset-2">
                <div class="form-group">
                    <label asp-for="RoleType" class="control-label"></label>
                    <input asp-for="RoleType" class="form-control" readonly/>
                    <span asp-validation-for="RoleType" class="text-danger"></span>
                </div>
                <div class="form-group mt-2">
                    <a class="btn btn-outline-success float-end" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
                    <a class="btn btn-outline-secondary" asp-action="Index">Show all</a>
                </div>
            </div>
        </div>
    </div>
</div>

